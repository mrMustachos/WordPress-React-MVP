import React from 'react';

const SearchedPage = (props) => {
	console.log('from SearchedPage.jsx: ', props);
	return (<div className="content-area">Howdy From the Search or the Category Page!</div>);
}

export default SearchedPage;